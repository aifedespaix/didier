{
  "name": "@aife/netcode",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aife/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/ws": "^8.5.10",
    "vitest": "^1.6.0",
    "ws": "^8.17.0",
    "fflate": "^0.8.2"
  }
}
